hello word !!

git --version 
git init --> give us a master 
git status -->status of repository
git init -b main --> gives us a main
when we make a file it adds it in working directory we have to add it to working area (Staging area) 
git add -_> adds a file in working area 
git log --> commit history  
git commit -m "XXXX"(when we want to commit we have to do it with a message  
every time we change a file first we have to bring it to staging area and after that commit and we do it with git add filename
if we wanted to scape the staging part we can do this : git commit -a -m "message"
with git diff it will show you the changes int he file 
if the file is in staging area we ahve to use git diff --staged
the git uses .md for readme file it makes it better 
and U beside the file means untracked 
M mean modified 
with git add we can add all the files by their names ot we can do git add .(dot) for all files 
the letter beside the file will change to A which means added
if we deleted a file amnualy it doenst get deleted from working directory we have to delete it fromt he git 
for deleting : git rm --chached file name --> after that we can delet the file 

for downloading from git hun we can use http we can use git clone and past the address

for making a remote repsetory : 
echo "# git-course" >> README.md  -> echo will make this messafe ( git course) to the readme file --> after that with cat file name we can read the content 
git init
git add README.md
git commit -m "first commit"
git branch -M main --> here is for getting the main branch 
git remote add origin https://github.com/sajjadnabi/git-course.git / this is for SSH git@github.com:sajjadnabi/git-course.git see ***
git push -u origin main --> here we are pushing from local to remote repository 


*** in order to avoid loging in every time --> ssh-keygen -o for generating 
then we do ls -a 
then we go to .ssh folder : cd .ssh 
then ls -Athen we open it : cat id_rsa.pub we find the key and then we copy the key 
then with control command we open uf seeting in github and go to ssh folder we click on new ssh and we past it there
then we do git remote 

for pushing we can use the szstanx --> git push origin main --> main is the branch 

origin : 
fo seeing origin we can use git remote -v
and we can connect to different origings 

Tag: 
it can be used for version for examplo 
git tag shows us the tags --> we have 2 options here : annotated tagging and lightweight tagging
for users we use annotated taging so we use option -a and -m is for message --> git tag -a XXX V1.0 -m "firsst version"
with git show then version we can see detail information about that particular tag 
we also need to push the tags seperately 
for that git push origin version

CLONING A PROJECT:
we can find our repository and then go to http nad we can also do it int erminal 
git clone copy past th url 
after opening the file in vs code 
in order to get particular information about a log we canw rite : 
git log --pretty=oneline ->> we will see commit and the messages 
when its particular path we should use git add path and file (if we ar enot there)

GIT BRANCH:
we want to creat a branch git checkout which is old the new one is 
git switch -c"name of branch" or git checkout -b "new branch"
for switching 
git switch "branch name"
with git branch we can check we we are -> the one with star is active
git branch --all -_> we can see al the branches inclusive remote 
git switch - -_> we will get back to our previous branch 
git branch -d --> for deleting the branch 
git push origin new_branch --> for pushing it into repository 
with git log --graph we can see the graph 
we can also install git graph 
for merging the branches we have to be on main branch and we use git merge
befor psuhin its better to do the pull 
git pull origin "branch name"

REBASE:
for  merging we can use merge and rebase 
in rebase we dont see the branches we sse a straight line 





